import React from "react";
import styled from "styled-components";

const CityWeather = styled.div`
  /* color: ${({ theme }) => theme.colors.white}; */
`;
const Weather = () => {
  return <CityWeather>Weather</CityWeather>;
};

export default Weather;
